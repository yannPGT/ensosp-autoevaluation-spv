import React,{FormEvent,useEffect,useMemo,useState}from"react";import{Niveau}from"./evaluation-data.js";import{UtilisateurCourant}from"./portal-data.js";import{ActionMetier,chargerDonneesOperationnelles,deciderAction,DecisionValidation,declarerProgression,demarrerAction,DonneesOperationnelles,journaliserConsultation,urlPieceJointe}from"./operational-data.js";
type Etat={statut:"chargement"}|{statut:"erreur";message:string}|{statut:"pret";donnees:DonneesOperationnelles};
const titres:Record<string,[string,string]>={progression:["Ma progression","Suivez vos actions et transmettez vos progrès à votre superviseur."],fiches:["Fiches d’enseignement","Consultez les ressources pédagogiques associées à vos actions."],historique:["Historique","Retrouvez vos évaluations et les décisions de progression."],recruteurs:["Mes recruteurs","Consultez les recruteurs visibles dans vos périmètres supervisés."],"gestion-recruteurs":["Gestion des recruteurs","Consultez les identités et rattachements que les ACL vous autorisent."],"evaluations-recruteurs":["Évaluations des recruteurs","Analysez les évaluations validées ou en cours de vos périmètres."],"progres-a-valider":["Progrès à valider","Traitez les demandes transmises par les recruteurs."],"progres-ouverts":["Progrès restant à valider","Suivez les résultats rouges et orange encore ouverts."],echeances:["Échéances et retards","Repérez les actions proches de leur échéance ou déjà dépassées."]};
titres.resultats=["Mes résultats","Consultez le bilan de votre dernière évaluation validée."];

export function ModuleOperationnel({page,utilisateur}:{page:string;utilisateur:UtilisateurCourant}){const[etat,setEtat]=useState<Etat>({statut:"chargement"}),[tentative,setTentative]=useState(0),[message,setMessage]=useState<{type:"succes"|"erreur";texte:string}|null>(null);useEffect(()=>{let actif=true;setEtat({statut:"chargement"});chargerDonneesOperationnelles().then(donnees=>{if(actif)setEtat({statut:"pret",donnees});}).catch(e=>{if(actif)setEtat({statut:"erreur",message:e instanceof Error?e.message:"Les données métier n’ont pas pu être chargées."});});return()=>{actif=false;};},[tentative]);if(etat.statut==="chargement")return<EtatOp texte="Chargement des données autorisées par Grist…"/>;if(etat.statut==="erreur")return<EtatOp texte={etat.message} action={()=>setTentative(x=>x+1)}/>;const notifier=(type:"succes"|"erreur",texte:string)=>setMessage({type,texte}),recharger=()=>setTentative(x=>x+1);return<section className="page-carte module-operationnel" aria-labelledby={`titre-${page}`}><div className="entete-module"><div><p className="surtitre">{utilisateur.role==="RECRUTEUR"?"Espace recruteur":"Supervision"}</p><h2 id={`titre-${page}`}>{titres[page]?.[0]||"Suivi opérationnel"}</h2><p>{titres[page]?.[1]}</p></div></div>{message&&<p className={`message-formulaire message-${message.type}`} role="status">{message.texte}</p>}<Vue page={page} d={etat.donnees} utilisateur={utilisateur} notifier={notifier} recharger={recharger}/></section>}

function Vue({page,d,utilisateur,notifier,recharger}:{page:string;d:DonneesOperationnelles;utilisateur:UtilisateurCourant;notifier:(t:"succes"|"erreur",x:string)=>void;recharger:()=>void}){if(page==="resultats")return<Resultats d={d}/>;if(page==="progression")return<Progression actions={d.actions} utilisateur={utilisateur} notifier={notifier} recharger={recharger}/>;if(page==="fiches")return<Fiches actions={d.actions} utilisateur={utilisateur} notifier={notifier}/>;if(page==="historique")return<Historique d={d}/>;if(page==="recruteurs"||page==="gestion-recruteurs")return<Recruteurs d={d}/>;if(page==="evaluations-recruteurs")return<Evaluations d={d}/>;if(page==="progres-a-valider")return<Validation actions={d.actions.filter(a=>a.statut==="EN_ATTENTE_VALIDATION"||a.statut==="PRISE_EN_COMPTE_DECLAREE")} utilisateur={utilisateur} notifier={notifier} recharger={recharger}/>;if(page==="progres-ouverts")return<ActionsOuvertes actions={d.actions.filter(a=>!(["PROGRESSION_VALIDEE","ARCHIVEE"]as string[]).includes(a.statut)&&a.niveauCourant!=="VERT")}/>;if(page==="echeances")return<Echeances actions={d.actions}/>;return null;}

function Resultats({d}:{d:DonneesOperationnelles}){const evaluation=d.evaluations.find(e=>e.statut==="VALIDEE");if(!evaluation)return<Vide texte="Aucune évaluation validée n’est disponible pour le moment."/>;const totaux:Record<Niveau,number>={ROUGE:0,ORANGE:0,VERT:0};evaluation.reponses.forEach(r=>totaux[r.niveau]++);return<div className="resultats-operationnels"><div className="synthese">{(["ROUGE","ORANGE","VERT"]as Niveau[]).map(n=><article className={`synthese-carte niveau-${n.toLowerCase()}`} key={n}><strong>{totaux[n]}</strong><span>{n}</span></article>)}</div><p>Évaluation validée le {evaluation.dateValidation} · {evaluation.perimetre}</p><div className="resultats-detail">{evaluation.reponses.map(r=><article className="resultat" key={r.id}><div><p><strong>{r.codeIndicateur}</strong> — {r.indicateur}</p>{r.commentaire&&<small>{r.commentaire}</small>}</div><span className={`badge niveau-${r.niveau.toLowerCase()}`}>{r.niveau}</span></article>)}</div></div>}

function Progression({actions,utilisateur,notifier,recharger}:{actions:readonly ActionMetier[];utilisateur:UtilisateurCourant;notifier:(t:"succes"|"erreur",x:string)=>void;recharger:()=>void}){if(!actions.length)return<Vide texte="Aucune action de progression n’est ouverte pour le moment."/>;return<div className="liste-actions-progres">{actions.map(a=><ActionRecruteur key={a.id} action={a} utilisateur={utilisateur} notifier={notifier} recharger={recharger}/>)}</div>}
function ActionRecruteur({action:a,utilisateur,notifier,recharger}:{action:ActionMetier;utilisateur:UtilisateurCourant;notifier:(t:"succes"|"erreur",x:string)=>void;recharger:()=>void}){const[commentaire,setCommentaire]=useState(a.commentaireRecruteur),[operation,setOperation]=useState(false);const agir=async(type:"DEMARRER"|"ENVOYER")=>{setOperation(true);try{if(type==="DEMARRER")await demarrerAction(a.id);else await declarerProgression(a.id,commentaire);notifier("succes",type==="DEMARRER"?"L’action est maintenant en cours.":"La demande de validation a été transmise.");recharger();}catch(e){notifier("erreur",e instanceof Error?e.message:"L’opération a échoué.");}finally{setOperation(false);}};return<article className="carte-action-progres"><EnteteAction action={a}/><p><strong>Échéance :</strong> {a.echeance}</p>{a.ficheVersionId&&<p className="ressource-associee">Ressource : <strong>{a.fiche}</strong> · version {a.version}</p>}<label>Progrès réalisés<textarea rows={3} value={commentaire} onChange={e=>setCommentaire(e.target.value)} disabled={a.statut==="EN_ATTENTE_VALIDATION"}/></label><div className="actions-formulaire">{a.statut==="A_PRENDRE_EN_COMPTE"&&<button type="button" className="bouton-secondaire" disabled={operation} onClick={()=>agir("DEMARRER")}>Prendre en compte</button>}{["A_PRENDRE_EN_COMPTE","EN_COURS","COMPLEMENT_DEMANDE","VALIDATION_REFUSEE"].includes(a.statut)&&<button type="button" disabled={operation} onClick={()=>agir("ENVOYER")}>Demander la validation</button>}{a.statut==="EN_ATTENTE_VALIDATION"&&<span>Demande transmise au superviseur.</span>}</div></article>}

function Fiches({actions,utilisateur,notifier}:{actions:readonly ActionMetier[];utilisateur:UtilisateurCourant;notifier:(t:"succes"|"erreur",x:string)=>void}){const fiches=actions.filter(a=>a.ficheVersionId&&a.attachmentId);if(!fiches.length)return<Vide texte="Aucune fiche publiée n’est actuellement affectée à vos actions."/>;const ouvrir=async(a:ActionMetier)=>{try{if(!a.ficheVersionId||!a.attachmentId)return;await journaliserConsultation(a.ficheVersionId,a,utilisateur,"OUVERTURE");const url=await urlPieceJointe(a.attachmentId);const lien=document.createElement("a");lien.href=url;lien.target="_blank";lien.rel="noopener";lien.click();}catch(e){notifier("erreur",e instanceof Error?e.message:"Le PDF n’a pas pu être ouvert.");}};return<div className="grille-fiches-recruteur">{fiches.map(a=><article key={a.id}><div><p className="surtitre">{a.codeIndicateur}</p><h3>{a.fiche}</h3><p>{a.indicateur}</p><small>{a.nomFichier} · version {a.version}</small></div><button type="button" onClick={()=>ouvrir(a)}>Consulter le PDF</button></article>)}</div>}

function Historique({d}:{d:DonneesOperationnelles}){return<div className="historique-metier"><section><h3>Évaluations</h3>{d.evaluations.length?d.evaluations.map(e=><article key={e.id}><div><strong>{e.dateValidation!=="—"?e.dateValidation:e.dateDebut}</strong><span className="badge-statut">{e.statut}</span></div><p>{e.reponses.length} réponse(s) · progression {e.progression}%</p></article>):<Vide texte="Aucune évaluation enregistrée."/>}</section><section><h3>Décisions de progression</h3>{d.validations.length?d.validations.map(v=><article key={v.id}><div><strong>{v.date}</strong><span className="badge-statut">{v.decision}</span></div><p>{v.ancienNiveau}{v.nouveauNiveau?` → ${v.nouveauNiveau}`:""} · {v.commentaire||"Sans commentaire"}</p></article>):<Vide texte="Aucune décision enregistrée."/>}</section></div>}
function Recruteurs({d}:{d:DonneesOperationnelles}){const liste=d.profils.filter(p=>p.role==="RECRUTEUR");if(!liste.length)return<Vide texte="Aucun recruteur n’est visible dans vos périmètres."/>;return<div className="liste-recruteurs-op">{liste.map(p=>{const evals=d.evaluations.filter(e=>e.recruteurId===p.id),actions=d.actions.filter(a=>a.recruteurId===p.id&&!(["PROGRESSION_VALIDEE","ARCHIVEE"]as string[]).includes(a.statut));return<article key={p.id}><div className="identite-utilisateur"><span className="avatar-utilisateur">{p.nom.split(" ").map(x=>x[0]).join("").slice(0,2)}</span><div><h3>{p.nom}</h3><p>{p.email}</p></div></div><dl><div><dt>Périmètre</dt><dd>{p.perimetre}</dd></div><div><dt>Évaluations</dt><dd>{evals.length}</dd></div><div><dt>Actions ouvertes</dt><dd>{actions.length}</dd></div></dl></article>})}</div>}
function Evaluations({d}:{d:DonneesOperationnelles}){const[selection,setSelection]=useState(d.evaluations[0]?.id??0);const e=d.evaluations.find(x=>x.id===selection);if(!d.evaluations.length)return<Vide texte="Aucune évaluation n’est visible dans vos périmètres."/>;return<div className="corps-evaluations-superviseur"><div className="liste-evaluations-superviseur">{d.evaluations.map(x=><button className={selection===x.id?"selectionnee":""} key={x.id} onClick={()=>setSelection(x.id)}><strong>{x.recruteur}</strong><span>{x.statut} · {x.progression}%</span><small>{x.dateValidation!=="—"?x.dateValidation:x.dateDebut}</small></button>)}</div>{e&&<article className="detail-evaluation-superviseur"><h3>{e.recruteur}</h3><p>{e.perimetre} · {e.statut}</p>{e.reponses.map(r=><div key={r.id}><span><code>{r.codeIndicateur}</code><strong>{r.indicateur}</strong></span><span className={`badge niveau-${r.niveau.toLowerCase()}`}>{r.niveau}</span></div>)}</article>}</div>}

function Validation({actions,utilisateur,notifier,recharger}:{actions:readonly ActionMetier[];utilisateur:UtilisateurCourant;notifier:(t:"succes"|"erreur",x:string)=>void;recharger:()=>void}){if(!actions.length)return<Vide texte="Aucune demande n’est actuellement en attente de votre décision."/>;return<div className="liste-validations-op">{actions.map(a=><FormDecision key={a.id} action={a} utilisateur={utilisateur} notifier={notifier} recharger={recharger}/>)}</div>}
function FormDecision({action:a,utilisateur,notifier,recharger}:{action:ActionMetier;utilisateur:UtilisateurCourant;notifier:(t:"succes"|"erreur",x:string)=>void;recharger:()=>void}){const[decision,setDecision]=useState<DecisionValidation>("VALIDEE"),[niveau,setNiveau]=useState<Niveau>(a.niveauCourant==="ROUGE"?"ORANGE":"VERT"),[commentaire,setCommentaire]=useState(""),[encours,setEncours]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setEncours(true);try{await deciderAction(a,decision,decision==="VALIDEE"?niveau:null,commentaire,utilisateur);notifier("succes","La décision a été enregistrée et historisée.");recharger();}catch(err){notifier("erreur",err instanceof Error?err.message:"La décision n’a pas pu être enregistrée.");}finally{setEncours(false);}};const niveaux:Niveau[]=a.niveauCourant==="ROUGE"?["ORANGE","VERT"]:["VERT"];return<form className="carte-action-progres" onSubmit={submit}><EnteteAction action={a}/><p><strong>Déclaration du recruteur :</strong> {a.commentaireRecruteur||"Aucun commentaire."}</p><div className="grille-decision"><label>Décision<select value={decision} onChange={e=>setDecision(e.target.value as DecisionValidation)}><option value="VALIDEE">Valider la progression</option><option value="COMPLEMENT_DEMANDE">Demander un complément</option><option value="REFUSEE">Refuser</option></select></label>{decision==="VALIDEE"&&<label>Nouveau niveau<select value={niveau} onChange={e=>setNiveau(e.target.value as Niveau)}>{niveaux.map(n=><option key={n}>{n}</option>)}</select></label>}</div><label>Commentaire<textarea rows={3} value={commentaire} onChange={e=>setCommentaire(e.target.value)} required={decision!=="VALIDEE"}/></label><button disabled={encours}>{encours?"Enregistrement…":"Enregistrer la décision"}</button></form>}
function ActionsOuvertes({actions}:{actions:readonly ActionMetier[]}){if(!actions.length)return<Vide texte="Aucun indicateur rouge ou orange n’est encore ouvert."/>;return<div className="liste-actions-progres">{actions.map(a=><article className="carte-action-progres" key={a.id}><EnteteAction action={a}/><p>{a.recruteur} · {a.perimetre}</p><p><strong>Échéance :</strong> {a.echeance}</p></article>)}</div>}
function Echeances({actions}:{actions:readonly ActionMetier[]}){const maintenant=Date.now()/1000,liste=[...actions].filter(a=>a.echeanceTimestamp&&!["PROGRESSION_VALIDEE","ARCHIVEE"].includes(a.statut)).sort((a,b)=>a.echeanceTimestamp-b.echeanceTimestamp);if(!liste.length)return<Vide texte="Aucune échéance active."/>;return<div className="liste-echeances">{liste.map(a=>{const retard=a.echeanceTimestamp<maintenant;return<article className={retard?"en-retard":""} key={a.id}><div><strong>{a.recruteur}</strong><span>{a.codeIndicateur} · {a.indicateur}</span></div><div><strong>{a.echeance}</strong><span>{retard?"En retard":"À venir"}</span></div></article>})}</div>}
function EnteteAction({action:a}:{action:ActionMetier}){return<div className="entete-action-progres"><div><code>{a.codeIndicateur}</code><h3>{a.indicateur}</h3></div><div><span className={`badge niveau-${a.niveauCourant.toLowerCase()}`}>{a.niveauCourant}</span><span className="badge-statut">{libelleStatut(a.statut)}</span></div></div>}function Vide({texte}:{texte:string}){return<p className="aucun-suivi">{texte}</p>}function EtatOp({texte,action}:{texte:string;action?:()=>void}){return<section className="page-carte etat-tableau"><p>{texte}</p>{action&&<button onClick={action}>Réessayer</button>}</section>}function libelleStatut(s:string){return s.replaceAll("_"," ").toLocaleLowerCase("fr").replace(/^./,x=>x.toLocaleUpperCase("fr"));}
